import { options } from '../actions'

export const FLIP_CARD = 'FLIP_CARD';
export const RESTART_GAME = 'RESTART_GAME';

export const flipCard = options.flipCard;
export const restart = options.restart;
